export enum PurchaseStatus {
  Order = "order",
  Invoice = "invoice",
  Processing = "processing",
}

export enum PurchaseDeliveryStatus {
  Packing = "packing",
  Custom = "custom",
  Storage = "storage",
}
